#include <stdio.h>
int main()
{
    int year;
    printf("Enter a year: ");
    scanf("%d",&year);
    if
    {
        if
        {
          
            if
                printf("%d is a leap year", year);
            else
                printf("%d is not a leap year", year);
        }
        else
            printf("%d is a leap year", year );
    }
    else
        printf("%d is not a leap year", year);
    
    return 0;
}
